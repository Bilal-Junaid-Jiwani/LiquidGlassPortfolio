{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist/public",
  "routes": [
        {
          "src": "/api/(.*)",
          "dest": "api/index.ts"
        },
        {
          "src": "/(.*\\.[a-zA-Z0-9]+$)",
          "dest": "/$1"
        },
        {
          "src": "/(.*)",
          "dest": "/index.html"
        }
      ]  }
